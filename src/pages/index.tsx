import React from "react";
import type { NextPage } from "next";
import { Layout } from "@components/common";
import { Heading } from "@components/core";

const Home: NextPage = () => {
  return (
    <Layout>
      <Heading>Helllo</Heading>
    </Layout>
  );
};

export default Home;
